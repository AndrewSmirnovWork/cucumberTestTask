package actualModels;

import baseModels.BaseModel;

public class DebitCard extends BaseModel {
    public DebitCard(int balance, String currency, String name) {
        super(balance, currency, name);
    }
}
